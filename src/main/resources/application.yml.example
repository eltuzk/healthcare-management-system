spring:
  application:
    name: healthcare-backend

  # DATABASE — Oracle
  datasource:
    # Format: jdbc:oracle:thin:@<host>:<port>/<service_name>
    # Ví dụ:  jdbc:oracle:thin:@localhost:1521/XEPDB1
    url: YOUR_ORACLE_JDBC_URL
    username: YOUR_DB_USERNAME       # Ví dụ: healthcare
    password: YOUR_DB_PASSWORD
    driver-class-name: oracle.jdbc.OracleDriver

  # JPA / HIBERNATE
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

  # FLYWAY — Database migration
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

  # MAIL — Gmail SMTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: YOUR_GMAIL_ADDRESS     # Ví dụ: yourapp@gmail.com
    password: YOUR_GMAIL_APP_PASSWORD
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# SWAGGER UI
springdoc:
  swagger-ui:
    path: /swagger-ui.html
